What's new in 4.0.0.2:

AI panel — home and remote access
- Default panel URL is the public address (port-forwarded server); office LAN and localhost are fallbacks
- Desks at home reach Supey / server geo without manual config

Schedule Builder
- Add row above / below submenu (mirrors Add note)
- Trailing blank rows at end of each driver tab for paste and insert

Analyzer
- Correctly counts Schedule Builder note rows (column A labels and template instructions)

Includes all 4.0.0.1 features (position notes, note colors, grid lines, bundled office Gmail defaults).