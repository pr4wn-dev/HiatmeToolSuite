What's new in 4.0.0.48:

Desktop shortcuts
- After update, desk shortcuts are rewritten to the install that was just patched
- Apply Latest will not treat the Desktop itself as the install folder (that dumped DLLs next to every shortcut)
- Old Hiatme .lnk files that still pointed at a leftover copy are retargeted