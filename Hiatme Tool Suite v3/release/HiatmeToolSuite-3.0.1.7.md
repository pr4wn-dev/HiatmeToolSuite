What's new in 3.0.1.7:

- Schedules: default save/load folder is Desktop\SCHEDULES FOR {year} (per-user Desktop/OneDrive); legacy Schedule for {year} still works if present
- Schedule Builder: drag-reorder driver tabs with drop highlight; tab order saved in UI order
- Schedule Builder: office AI panel auto-discovery on LAN (no per-desk IP config); LAN desks connect without tokens
- Schedule Builder: map/routing no longer freezes the app when scanning the network; safer reconnect on tab open
- Analyzer / Templates: open-file dialogs start in the schedule year folder instead of a hardcoded user path
